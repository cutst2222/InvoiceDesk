[package]
name = "consultant_invoice_submission"
version = "0.1.0"
description = "Consultant Invoice Submission desktop shell"
authors = ["InvoiceDesk"]
license = "MIT"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.2", features = [] }

[dependencies]
tauri = { version = "2.2.5", features = [] }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
